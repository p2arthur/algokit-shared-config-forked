name: "Publish Documentation"
description: "A composite action to upload and deploy documentation artifacts to GitHub Pages."

inputs:
runs:
  using: "composite"
  steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
